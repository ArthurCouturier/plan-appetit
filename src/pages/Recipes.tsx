import { HomeButton } from "../components/buttons/MenuButton";

export default function Recipes() {
    return (
        <div>
            <HomeButton />
            <h1>Recettes</h1>
        </div>
    );
}